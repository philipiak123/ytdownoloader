<!DOCTYPE html>
<html lang="pl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pobieranie zakończone</title>
    <style>
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
        }

        body {
            background-color: #f4f4f4;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .container {
            background: #ffffff;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.1);
            width: 100%;
            max-width: 400px;
            text-align: center;
        }

        h1 {
            font-size: 20px;
            margin-bottom: 15px;
            color: #333;
        }

        .download-btn {
            width: 100%;
            padding: 12px;
            background: #28a745;
            border: none;
            color: white;
            font-size: 16px;
            border-radius: 5px;
            cursor: pointer;
            transition: 0.3s;
        }

        .download-btn:hover {
            background: #218838;
        }

        .back-btn {
            width: 100%;
            padding: 10px;
            background: #dc3545;
            border: none;
            color: white;
            font-size: 14px;
            border-radius: 5px;
            cursor: pointer;
            margin-top: 10px;
            transition: 0.3s;
        }

        .back-btn:hover {
            background: #c82333;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Twój plik jest gotowy do pobrania!</h1>
        <button class="download-btn" id="downloadButton">📥 Pobierz plik</button>
    </div>

    <script>
        document.getElementById('downloadButton').addEventListener('click', function() {
            const filePath = "<%= filePath %>";
            const fileName = filePath.split('/').pop();

            // Tworzenie linku do pobrania pliku
            const link = document.createElement('a');
            link.href = "/" + filePath;
            link.download = fileName;
            document.body.appendChild(link);
            link.click();
            document.body.removeChild(link);

            // Przekierowanie po 5 sekundach na stronę główną
            setTimeout(() => {
                window.location.href = "/";
            }, 5000);
        });
    </script>
</body>
</html>
